## 3. 理解度確認クイズ

ここまでの内容が理解できているか、頭の中で整理してみましょう！

### 第1問（選択式）
テーブルに新しくデータを「追加（登録）」するときに使用するSQLコマンドはどれですか？
- **C.** `INSERT`

### 第2問（記述式）
`UPDATE` や `DELETE` を実行するとき、条件（どこのレコードを書き換えるか）を指定する `WHERE句` を書き忘れてしまうと、データベースでは何が起きてしまいますか？簡単に説明してください。

すべて書き換わったりすべて削除されてしまったりする。

### 第3問（穴埋め式）
「データを保存されているIDが大きい順（5, 4, 3...）に並び替えたい」とき、以下の（　）に入るキーワードを答えてください。
`SELECT * FROM todos ORDER BY id (   DESC   );`